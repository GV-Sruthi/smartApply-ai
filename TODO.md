# TODO

- [ ] Fix frontend dev server startup failure by removing or installing missing Tailwind plugin (`@tailwindcss/vite`).
- [ ] Restart frontend `npm run dev` to confirm Vite boots.
- [ ] Run backend (if not already) and verify `/analyze` endpoint accepts form-data.
- [ ] Re-check for runtime UI errors by exercising the Analyze flow.

